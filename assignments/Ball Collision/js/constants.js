const container = document.querySelector("#container");

// Initializing and declaring different variables and constants
const ballArray = [];
const noOfBalls = 100;
const ballRadiusRange = {
  min: 10,
  max: 20,
};
const ballVelocityRange = {
  min: 1,
  max: 4,
};

const colorArray = ["#5ADBFF", "#FFDD4A", "#094074", "#FE9000", "#53DD6C"];
